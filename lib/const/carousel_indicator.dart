import 'package:flutter/material.dart';

class CIndicator extends StatelessWidget {
  const CIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
